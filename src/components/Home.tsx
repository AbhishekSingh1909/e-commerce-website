import { Box, Typography } from "@mui/material";

const Home = () => {
  return (
    <main>
      <Box>
        <Typography variant="h4">Home</Typography>
      </Box>
    </main>
  );
};
export default Home;
