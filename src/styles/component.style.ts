import { ButtonGroup, Card } from "@mui/material";
import { styled } from "@mui/material/styles";

const NavButtonGroup = styled(ButtonGroup)(({ theme }) => ({
  boxShadow: "none",
}));

export { NavButtonGroup };
