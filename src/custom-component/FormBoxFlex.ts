import { Box, styled } from "@mui/material";

const FormBoxFlex = styled(Box)(({ theme }) => ({
  marginTop: 8,
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  color: "CaptionText",
}));

export default FormBoxFlex;
